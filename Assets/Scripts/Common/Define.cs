namespace Define
{
    public enum SceneIndex
    {
        Title,
        In_Game,
        Loading,
    }

    public enum AdType
    {
        Banner,
        Interstitial,
        Reward,
    }

    public enum SFX
    {
        jump, slide
    }

    public enum BGM
    {
        MainScene
    }
}